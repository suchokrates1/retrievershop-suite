[tool.pytest.ini_options]
testpaths = ["magazyn/tests"]
markers = [
    "e2e: testy E2E w przegladarce (Playwright)",
    "ui: testy UI Playwright z lokalnym serwerem Flask",
    "snapshot: porownanie zrzutow ekranu (wrazliwe na OS/rendering)",
]
